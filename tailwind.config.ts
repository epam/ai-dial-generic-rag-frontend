import type { Config } from 'tailwindcss';

const config: Config = {
  theme: {
    extend: {
      colors: {
        blackout: '#131722',
        highlight: '#e7e9ec',
      },
    },
  },
};

export default config;
